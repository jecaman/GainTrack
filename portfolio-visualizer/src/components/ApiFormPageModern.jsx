import ApiFormModern from './ApiFormModern';

const ApiFormPageModern = ({ onSubmit, isLoading, error }) => (
  <ApiFormModern onSubmit={onSubmit} isLoading={isLoading} error={error} />
);

export default ApiFormPageModern;